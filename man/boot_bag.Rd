% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-bag.R
\name{boot_bag}
\alias{boot_bag}
\title{A screen the reporting layer can read}
\usage{
boot_bag(x, base_params, requested, manifest, dropped = NULL, usable = NULL)
}
\arguments{
\item{x}{A \code{boot_selection} from \code{\link[=boot_select]{boot_select()}}, run under 0.9.2 or later.
An object from an earlier version carries no \verb{$control} and is refused by
name rather than failing on an absent list element.}

\item{base_params}{Character. The terms that are the base model rather than
candidates. They are excluded from every frequency, and the first of them
is the parameter \code{\link[=boot_health]{boot_health()}} watches for a zero standard deviation.
Must name terms the screen carries. \strong{A Cox model has no intercept}, so
\code{"(Intercept)"} copied from a logistic runner names nothing and is refused.}

\item{requested}{Numeric. How many candidates the runner \strong{offered}, before
it dropped any. Not derivable here: a candidate dropped before screening
never became a column.}

\item{manifest}{A named list describing the dataset screened, indexed by
name. \code{list(sha256 = ...)} at minimum.}

\item{dropped}{Optional data frame of candidates dropped before screening, as
\code{\link[=boot_dropped]{boot_dropped()}} reports it. Absent means nothing was dropped.}

\item{usable}{Optional numeric, checked rather than used. Supply it to assert
the count the runner believed it screened; a disagreement with the screen's
own term count is refused, because one of the two is describing a different
run.}
}
\value{
A list carrying the fields \code{\link[=boot_validate]{boot_validate()}} requires: \code{n_boot},
\code{seed}, \code{slentry}, \code{slstay}, \code{base_params}, \code{requested}, \code{usable},
\code{n_rows}, \code{elapsed_mins}, \code{manifest}, \code{engine}, \code{dropped} when supplied,
and \code{boot} holding \code{replicates}, \code{summary}, \code{n_success} and \code{n_failed}.
\code{boot$summary} is keyed \code{parameter} -- as \code{boot$replicates} and
\code{\link[=boot_health]{boot_health()}} are, and as \code{\link[=boot_pool_chunks]{boot_pool_chunks()}} returns -- rather than
\code{\link[=boot_summary]{boot_summary()}}'s \code{variable}, and carries \code{sel_q025} and \code{sel_q975}
alongside the \verb{\%SUMBOOT} statistics. Those two are the spread of the
coefficient across the replicates that \strong{selected} it, not a confidence
interval: a term chosen half the time has an interval over half the
replicates.
Validated before it is returned, so this function cannot emit a bag that a
report will refuse three chunks into a render.
}
\description{
Convert a \code{\link[=boot_select]{boot_select()}} result into the bag that \code{\link[=boot_validate]{boot_validate()}} accepts
and every reporting function reads.
}
\details{
\code{\link[=boot_select]{boot_select()}} returns a \strong{wide} object: one row per replicate, one column
per term, \code{NA} where a term was not selected. The reporting layer reads a
\strong{long} one, because it was extracted from a hazard runner that writes long.
Nothing converted between them, so this package's own screen function could
not reach its own report and every study would have hand-written the pivot
plus nine provenance fields. This is that conversion, written once.

\strong{The pivot drops \code{NA} rather than writing it.} That is not an optimisation:
\code{\link[=boot_frequencies]{boot_frequencies()}} counts a term's rows against \code{n_boot}, so a row written
for an unselected term would be counted as a selection and every frequency
would rise. The replicate count travels in \code{n_boot}, never in the row count.

Everything the run knows about itself comes from \code{x$control} rather than from
an argument, so a bag cannot claim an entry level the screen did not use. The
arguments here are the facts \code{\link[=boot_select]{boot_select()}} cannot know: which terms are the
base model, how many candidates existed before the runner dropped any, what
dataset was screened, and what was dropped.
}
\examples{
set.seed(1)
n  <- 200
x1 <- rnorm(n)
df <- data.frame(y = 2 * x1 + rnorm(n), x1 = x1, x2 = rnorm(n))
fit <- boot_select(df, y ~ x1 + x2, fit_linear, n_rep = 10, seed = 42)
bag <- boot_bag(fit, base_params = "(Intercept)", requested = 2,
                manifest = list(sha256 = "example"))
boot_frequencies(bag)

}
\seealso{
\code{\link[=boot_select]{boot_select()}} for the screen, \code{\link[=boot_provenance]{boot_provenance()}} and
\code{\link[=boot_frequencies]{boot_frequencies()}} for what reads the result.
}
