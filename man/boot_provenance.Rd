% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-provenance.R
\name{boot_provenance}
\alias{boot_provenance}
\title{Where a bootstrap screen came from}
\usage{
boot_provenance(bag)
}
\arguments{
\item{bag}{A bootstrap screen: the object \code{\link[=boot_pool_chunks]{boot_pool_chunks()}} returns, or a
single unchunked run of the same shape. Checked by \code{\link[=boot_validate]{boot_validate()}}.}
}
\value{
A data frame with columns \code{item} and \code{value}, both character, one
row per fact. \strong{The row count does not depend on the shape of any field}
\itemize{
\item a per-phase \code{requested} yields the same rows as a scalar one.
}
}
\description{
The facts a reader needs to judge, reproduce or reject a screen: how many
replicates, under which entry and stay criteria, over how many candidates
and rows, at what cost, against which dataset and which build of the fitting
engine.
}
\details{
\code{requested} and \code{usable} are \strong{per phase} on a multiphase screen, and
rendering them is the reason this function exists rather than a bare
\code{data.frame()} call in each report. They are collapsed to one labelled
string per row --
\code{"early 230, late 230"} -- and never summed: the candidate pool is \emph{offered}
to each phase, so 230 and 230 is one pool seen twice rather than 460
candidates.

Two facts travel with a tag that says what kind of evidence they are:
\itemize{
\item The \strong{dataset checksum} is \code{"<algo>:<digest>"}. An md5 and a sha256 of
the same file are different strings, and of different files may not be, so
a bare digest recorded without its algorithm is not evidence of anything.
\item The \strong{fitting engine} is \code{"sha:<commit>"} in preference to
\code{"version:<string>"}. One real package version existed as two codebases,
one with a selection criterion and one without, and the selection
criterion is precisely the thing that decides what a screen selects.
}

Elapsed time is reported as \strong{summed CPU hours}, not wall clock:
\code{\link[=boot_pool_chunks]{boot_pool_chunks()}} sums \code{elapsed_mins} across chunks, so on a chunked run
this is total compute and chunks run in parallel finish in a fraction of it.

Seeds are summarised here and listed by \code{\link[=boot_seeds]{boot_seeds()}}. At 25 chunks the
joined seed string is a single 270-character cell.
}
\examples{
bag <- list(
  n_boot = 500L, n_chunks = 2L, seed = "101, 202", seeds = c(101, 202),
  slentry = 0.07, slstay = 0.05, base_params = "base",
  requested = c(early = 230L, late = 230L),
  usable = c(early = 226L, late = 226L),
  n_rows = 4000L, elapsed_mins = 150, th_sha = "deadbeef",
  manifest = list(sha256 = "abc123"),
  boot = list(replicates = data.frame(replicate = 1L, parameter = "base",
                                      estimate = 1),
              summary = data.frame(parameter = "base", n = 1L, pct = 100),
              n_success = 500L, n_failed = 0L))
boot_provenance(bag)
boot_seeds(bag)
}
\seealso{
\code{\link[=boot_seeds]{boot_seeds()}} for the seeds themselves, \code{\link[=boot_health]{boot_health()}} for
whether the screen ran, and \code{\link[=boot_shortfall]{boot_shortfall()}} for whether the pool is the
run that was launched.
}
