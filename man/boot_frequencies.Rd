% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-frequencies.R
\name{boot_frequencies}
\alias{boot_frequencies}
\title{Selection frequencies, with the error they carry}
\usage{
boot_frequencies(bag, phase = NULL, threshold = NULL)
}
\arguments{
\item{bag}{A bootstrap screen. Checked by \code{\link[=boot_validate]{boot_validate()}}.}

\item{phase}{A function mapping a term to its phase, or \code{NULL}. With \code{NULL}
there is no phase dimension and no \code{phase} column. A multiphase hazard
screen passes \code{function(term) sub("[.].*$", "", term)}, which reads
\code{early.age} as the early phase's decision about \code{age}; a logistic, linear
or Cox screen has no phases and passes nothing. The rule is applied one
term at a time, so it need not be vectorised, and \code{variable} is the term
with the returned phase label stripped from its head when it is there.}

\item{threshold}{The retention cutoff, as a percentage, or \code{NULL}. This is a
\strong{reporting} decision and not something the run recorded: \code{slentry} and
\code{slstay} governed each replicate's stepwise fit, and neither says how
often a variable must survive to be worth carrying forward.}
}
\value{
A data frame with columns \code{variable}, \code{term}, \code{n}, \code{pct},
\code{mc_error}, \code{near_threshold} and \code{retained}, plus \code{phase} when \code{phase} is
supplied. \code{term} is the name as the screen recorded it and \code{variable} the
same name without its phase; they are identical when there is no phase
rule. \code{near_threshold} marks a term within two Monte-Carlo errors of the
cutoff -- retention that would not survive a rerun with different seeds --
and both it and \code{retained} are \code{NA} when no \code{threshold} is given, because
with no cutoff there is no retention decision to make a claim about.
}
\description{
How often each candidate term survived selection, and how much of that
figure is noise.
}
\details{
A selection frequency is an \strong{estimate}, not a count of something fixed. It
carries Monte-Carlo error of roughly \code{sqrt(p(1 - p) / n_boot)}, which at
\code{p = 0.5} over 500 replicates is about 2.2 percentage points -- so a
variable sitting within a few points of the retention cutoff can fall on
either side of it on resampling noise alone.

\code{mc_error} is computed \strong{per variable}, not once for the table. The error
is largest at \code{p = 0.5} and shrinks towards either end, so a single quoted
value overstates it for the variables at the top and understates it in the
middle -- and the middle is where the retention decision is being made.

\code{n} and \code{pct} come from \code{\link[=boot_summary]{boot_summary()}}, which is the function held to
exact parity with \verb{\%SUMBOOT}. They are not recomputed here: a second
implementation of the selection count is the one thing this package cannot
afford. What this function adds is the error, the cutoff and the optional
phase grouping.

\strong{The base parameters are dropped.} They are in every replicate by
construction, and reporting them at 100\% invites them to be read as the
screen's most reliable findings.
}
\examples{
bag <- list(
  n_boot = 4L, seed = 101, slentry = 0.07, slstay = 0.05,
  base_params = "base", requested = 4L, usable = 3L, n_rows = 500L,
  elapsed_mins = 120, manifest = list(sha256 = "abc123"),
  boot = list(
    replicates = data.frame(
      replicate = c(1L, 1L, 2L, 3L, 4L),
      parameter = c("base", "early.age", "early.age", "base", "late.bmi"),
      estimate  = c(1, 0.5, 0.6, 0.9, 0.2)),
    summary = data.frame(parameter = "base", n = 2L, pct = 50),
    n_success = 4L, n_failed = 0L))

boot_frequencies(bag)

# A multiphase screen passes its own term-splitting rule.
boot_frequencies(bag, phase = function(term) sub("[.].*$", "", term),
                 threshold = 50)
}
\seealso{
\code{\link[=boot_summary]{boot_summary()}}, which computes the counts; \code{\link[=boot_concepts]{boot_concepts()}} for
the same frequencies grouped by concept; \code{\link[=boot_dropped]{boot_dropped()}} for the
candidates that never reached a replicate at all.
}
