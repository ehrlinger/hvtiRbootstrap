% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-intervals.R
\name{boot_predict_ci}
\alias{boot_predict_ci}
\title{Band an estimate by bootstrap resampling}
\usage{
boot_predict_ci(
  data,
  statistic,
  n_rep = 1000,
  fraction = 1,
  id = NULL,
  max_attempts = 10 * n_rep,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{statistic}{Function of \verb{(data, ...)} returning a named numeric vector
of one replicate's estimates, or \code{NULL} if the replicate failed.
\verb{\%BNMNR} equivalent: the \verb{PROC NLMIXED} block.}

\item{n_rep}{Number of \strong{valid} replicates (\verb{\%BNMNR} \verb{RESAMPL=}).
\strong{Divergence:} the macro defaults to 100; this defaults to 1000,
matching \code{\link[=boot_select]{boot_select()}} in this package. A hundred replicates puts the
2.5th percentile on the third order statistic, where it is visibly
unstable. Pass \code{n_rep = 100} to reproduce the macro.}

\item{fraction}{Fraction of units drawn per replicate (\verb{\%BNMNR}
\verb{FRACTION=}). Applied, as the macro applies it -- unlike \verb{\%bootreg},
which computes it and then draws the full size anyway.}

\item{id}{Column naming the resampling unit, or \code{NULL} to draw rows. When
given, units are drawn with replacement and \strong{renumbered}, so a unit
drawn twice becomes two distinct units; the new id is in a \code{.boot_unit}
column. \verb{\%BNMNR} equivalent: the patient-level \verb{INDAT=} joined to the
repeated \verb{INMULT=}.}

\item{max_attempts}{Budget of draws before giving up. \strong{Divergence:}
\verb{\%BNMNR} has no cap and never terminates when every replicate fails.
Pass \code{Inf} to restore that.}

\item{seed}{Optional integer for reproducibility (\verb{\%BNMNR} \verb{SEED=}).}

\item{...}{Passed to \code{statistic}.}
}
\value{
An object of class \code{boot_intervals}. \verb{$estimates} is a matrix with
one row per valid replicate and one column per quantity; \verb{$intervals} is
the per-quantity table with columns \code{parameter}, \code{cll_p95}, \code{cll_p68},
\code{median}, \code{clu_p68}, \code{clu_p95}. \verb{$control} records the run's settings.
}
\description{
The R port of \verb{\%BNMNR} and \verb{\%BNPREV} (\verb{bn.*} in the CORR macro library).
Resamples \code{data}, computes \code{statistic} on each replicate, and reports the
percentile intervals of each estimated quantity.
}
\details{
This is the \strong{interval} branch. It is not variable selection: nothing is
chosen, the replicates are a distribution rather than a vote, and there is
no \code{NA} semantics. For selection, see \code{\link[=boot_select]{boot_select()}}.

\code{statistic} is the fitter contract with the selection semantics removed. It
takes one resampled frame and returns a \strong{named numeric vector} of that
replicate's estimates, or \code{NULL} when the replicate did not fit. The names
are the quantities banded, so a caller wanting a curve evaluates it on
their own grid inside \code{statistic} and names the elements -- which is what
the macro does too, in a \verb{PROC NLMIXED} block the analyst edits.

\strong{The bands are pointwise.} Each quantity is summarised independently, as
\verb{\%BNMNR} does. A curve drawn through \code{cll_p95} is the 2.5th percentile at
each point, not a 95\% region for the curve.

No argument sets a coverage level, here or anywhere in this package. The
macros do not take one either: they hardcode
\verb{PCTLPTS=2.5 16 50 84 97.5} and return both bands in columns named for
their coverage. Percentiles use \code{\link[stats:quantile]{stats::quantile()}} \code{type = 4}, which is
SAS's \code{PCTLDEF=1}.
}
\examples{
# The names of the returned vector are the quantities banded. Here, two
# summaries of the same column; in a real screen they are typically a
# fitted curve evaluated on a grid.
df <- data.frame(x = rnorm(200))
est <- function(d, ...) c(mean = mean(d$x), sd = sd(d$x))

r <- boot_predict_ci(df, est, n_rep = 100, seed = 42)
r
summary(r)
}
\seealso{
\code{\link[=boot_select]{boot_select()}} for the selection branch.
}
