% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-pool.R
\name{boot_pool_chunks}
\alias{boot_pool_chunks}
\title{Pool chunked bootstrap runs into one screen}
\usage{
boot_pool_chunks(chunks)
}
\arguments{
\item{chunks}{A list of chunk objects, each carrying at least \code{seed},
\code{n_boot}, \code{slentry}, \code{slstay}, \code{base_params}, \code{usable}, \code{max_steps},
\code{elapsed_mins}, a \code{manifest} with an \code{md5} or \code{sha256}, an engine
provenance field (\code{th_sha} or \code{th_version}), and a \code{boot} list with
\code{replicates}, \code{n_success} and \code{n_failed}.}
}
\value{
One object shaped like a single chunk, with \code{boot$replicates} stacked
and re-indexed, \code{boot$summary} recomputed, and \code{n_chunks}, \code{seeds} and a
joined \code{seed} string added.
}
\description{
Folds a list of chunk objects into a single object of the same shape, so a
report reads a pooled screen and a single-run screen the same way.
}
\details{
Six things must agree for chunks to be draws from one screen, and each is
checked rather than assumed. \strong{None of them fails loudly on its own:}
\itemize{
\item Two chunks sharing a \strong{seed} contain literally the same replicates.
Pooling them counts each twice, halves nothing, and reports a Monte-Carlo
error smaller than the run actually has.
\item A \strong{dataset rewritten mid-run} gives chunks that each look fine and
describe different cohorts.
\item A different \strong{step cap} changes the screen, not just its cost: under a
tighter cap a variable that would have entered late never gets the chance.
}

Neither shows up as an error in a frequency table. Both show up as a slightly
different number.

\strong{Absence is checked before agreement, and that order is the whole point.} A
field no chunk records formats identically in every chunk, so it passes
unanimously and the accessor hands back \code{NULL} as the agreed value. A gate
that cannot tell "everyone agrees" from "nobody recorded it" is not a gate,
and it is worse than having none, because it reads as a check that happened.
This was not hypothetical: \code{max_steps} was absent from every chunk in one
test fixture and the suite was green.

Replicate ids run \verb{1..n_boot} \strong{inside} each chunk, so they collide across
chunks and are offset before stacking. Without that, replicate 1 of chunk 2
merges into replicate 1 of chunk 1 and a variable selected in both counts
once instead of twice — understating every frequency, in a table that looks
entirely ordinary.

The pooled summary is \strong{recomputed from the pooled replicates}, never
averaged across chunks: a percentage of a percentage is not a percentage of
the whole, and the chunks need not be the same size.
}
\seealso{
\code{\link[=boot_chunk_files]{boot_chunk_files()}} to find them, \code{\link[=boot_shortfall]{boot_shortfall()}} to check the
pool is the run that was launched, and \code{\link[=boot_summary]{boot_summary()}} for the
per-variable frequencies.
}
