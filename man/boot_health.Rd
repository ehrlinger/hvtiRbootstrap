% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-health.R
\name{boot_health}
\alias{boot_health}
\title{Did the bootstrap screen actually run?}
\usage{
boot_health(bag)
}
\arguments{
\item{bag}{A bootstrap screen. Checked by \code{\link[=boot_validate]{boot_validate()}}.}
}
\value{
A data frame with one row per check and columns \code{check}, \code{value},
\code{ok} and \code{note}. \code{ok} is \code{FALSE} for a failed check, \code{TRUE} for a passed
one, and \code{NA} for a row that is a fact rather than a check. \code{note} says
what a failure means and is \code{NA} otherwise.
}
\description{
Four facts about a screen, two of which are checks that catch a run which
produced a perfectly ordinary-looking table of numbers that mean nothing.
}
\details{
\strong{A screen that selected nothing is a failure, not a finding.} It is the
signature of a formula that did not survive the per-replicate rewrite: the
refit errors, the error is caught, the step reports nothing accepted, and
the screen halts having selected nothing -- with no warning and
\code{n_failed = 0}. The summary then reads as a table of perfectly reliable
variables. Absence from a frequency table cannot say this; a row that says
\code{ok = FALSE} can.

\strong{A free base parameter must vary across resamples.} A bootstrap built on
a vector interface returns the original fit every replicate, with
\code{n_success = 500}, \code{n_failed = 0} and no warning at all. The standard
deviation of the first free base parameter is the only tell, and a value of
exactly zero is that failure. It is \code{NA}, never \code{0}, when there is nothing
to take a standard deviation of: zero is the \emph{claim} that the parameter did
not vary, and one replicate cannot support it.

\code{free_sd} is recomputed when the screen does not carry it. Only
\code{\link[=boot_pool_chunks]{boot_pool_chunks()}} writes that field, so a single unchunked run has
none -- and an unchunked run is the shape most likely to have been built the
way this check exists to catch. Skipping it there would turn the check off
in exactly the case that needs it.

This function \strong{returns} its findings rather than stopping. A report
decides how to present them: a callout, a red cell, or a \code{stop()} of its
own.
}
\examples{
bag <- list(
  n_boot = 4L, seed = 101, slentry = 0.07, slstay = 0.05,
  base_params = "base", requested = 4L, usable = 3L, n_rows = 500L,
  elapsed_mins = 120, manifest = list(sha256 = "abc123"),
  boot = list(
    replicates = data.frame(
      replicate = c(1L, 1L, 2L, 3L, 4L),
      parameter = c("base", "early.age", "base", "base", "base"),
      estimate  = c(1, 0.5, 1.1, 0.9, 1.2)),
    summary = data.frame(parameter = "base", n = 4L, pct = 100),
    n_success = 4L, n_failed = 0L))
boot_health(bag)

# A screen that selected nothing outside the base model.
bag$boot$replicates <- bag$boot$replicates[-2, ]
boot_health(bag)
}
\seealso{
\code{\link[=boot_provenance]{boot_provenance()}} for where the screen came from, and
\code{\link[=boot_shortfall]{boot_shortfall()}} for whether the pool is the run that was launched --
a different question, and one nothing inside a chunk can answer.
}
