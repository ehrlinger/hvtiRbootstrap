% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitters.R
\name{fit_cox}
\alias{fit_cox}
\title{Fit a Cox proportional-hazards model for one bootstrap replicate}
\usage{
fit_cox(data, formula, select)
}
\arguments{
\item{data}{A data frame - one bootstrap replicate.}

\item{formula}{Model formula offering the candidate terms.}

\item{select}{List with \code{method} (\code{"stepwise"} or \code{"none"}), \code{sle}, \code{sls},
\code{max_steps}. \verb{\%bootreg} equivalents: \verb{SELECT=}, \verb{SLE=}, \verb{SLS=}, \verb{MAXSTEP=}.}
}
\value{
Named numeric vector of kept coefficients, or \code{NULL} if the fit
errored. Cox models carry no intercept, so none appears in the result -
which means a replicate where selection kept nothing returns a
\strong{zero-length} vector, not \code{NULL}, and counts as a valid replicate.
}
\description{
Fit a Cox proportional-hazards model for one bootstrap replicate
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  set.seed(2)
  n  <- 200
  df <- data.frame(time = rexp(n), status = rbinom(n, 1, 0.7),
                   x1 = rnorm(n), noise = rnorm(n))

  fit_cox(df, survival::Surv(time, status) ~ x1 + noise,
          list(method = "stepwise", sle = 0.10, sls = 0.05, max_steps = 0))
}
}
\seealso{
\code{\link[=fit_linear]{fit_linear()}} and \code{\link[=fit_logistic]{fit_logistic()}}; \code{\link[=boot_select]{boot_select()}}.
}
