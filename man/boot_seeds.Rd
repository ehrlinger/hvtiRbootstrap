% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-provenance.R
\name{boot_seeds}
\alias{boot_seeds}
\title{Every seed a bootstrap screen used}
\usage{
boot_seeds(bag)
}
\arguments{
\item{bag}{A bootstrap screen. Checked by \code{\link[=boot_validate]{boot_validate()}}.}
}
\value{
A data frame with columns \code{chunk} (integer) and \code{seed} (character).
}
\description{
One row per chunk, so that a rerun is reproducible and a duplicate is
visible.
}
\details{
Two chunks sharing a seed contain literally the \strong{same} replicates: pooling
them counts each twice and reports a Monte-Carlo error smaller than the run
actually has. \code{\link[=boot_pool_chunks]{boot_pool_chunks()}} refuses that outright, so a pooled screen
cannot reach here with a duplicate. This table is what lets you check a
\emph{single} run, and what lets a reader reproduce either.

A single unchunked run never went through \code{\link[=boot_pool_chunks]{boot_pool_chunks()}} and carries
only the scalar \code{seed} its runner wrote, so that is used when \code{seeds} is
absent. Seeds are formatted without scientific notation: a seed printed as
\code{1.23e+08} cannot be typed back in.
}
\examples{
bag <- list(
  n_boot = 500L, seed = 4242, slentry = 0.07, slstay = 0.05,
  base_params = "base", requested = 230L, usable = 226L,
  n_rows = 4000L, elapsed_mins = 150, manifest = list(sha256 = "abc123"),
  boot = list(replicates = data.frame(replicate = 1L, parameter = "base",
                                      estimate = 1),
              summary = data.frame(parameter = "base", n = 1L, pct = 100),
              n_success = 500L, n_failed = 0L))
boot_seeds(bag)
}
\seealso{
\code{\link[=boot_provenance]{boot_provenance()}}, which summarises the count.
}
