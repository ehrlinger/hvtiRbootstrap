% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitters.R
\name{fit_logistic}
\alias{fit_logistic}
\title{Fit a logistic model for one bootstrap replicate}
\usage{
fit_logistic(data, formula, select)
}
\arguments{
\item{data}{A data frame - one bootstrap replicate.}

\item{formula}{Model formula offering the candidate terms.}

\item{select}{List with \code{method} (\code{"stepwise"} or \code{"none"}), \code{sle}, \code{sls},
\code{max_steps}. \verb{\%bootreg} equivalents: \verb{SELECT=}, \verb{SLE=}, \verb{SLS=}, \verb{MAXSTEP=}.}
}
\value{
Named numeric vector of kept coefficients, or \code{NULL} if the fit
errored or did not converge. Warnings - notably "fitted probabilities
numerically 0 or 1" on a quasi-separated replicate - do not discard a
converged fit.
}
\description{
Fit a logistic model for one bootstrap replicate
}
\details{
Under \code{select = "stepwise"}, entry tests the score chi-square and removal
tests Wald, matching \verb{PROC LOGISTIC SELECTION=STEPWISE}.
}
\examples{
set.seed(1)
n  <- 200
x1 <- rnorm(n)
df <- data.frame(y = as.integer(x1 + rnorm(n) > 0), x1 = x1,
                 noise = rnorm(n))

fit_logistic(df, y ~ x1 + noise,
             list(method = "stepwise", sle = 0.10, sls = 0.05,
                  max_steps = 0))
}
\seealso{
\code{\link[=fit_linear]{fit_linear()}} and \code{\link[=fit_cox]{fit_cox()}}; \code{\link[=boot_select]{boot_select()}}.
}
