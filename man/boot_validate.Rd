% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-validate.R
\name{boot_validate}
\alias{boot_validate}
\title{Check that a bootstrap bag has the shape a report reads}
\usage{
boot_validate(bag)
}
\arguments{
\item{bag}{A bootstrap screen: the object \code{\link[=boot_pool_chunks]{boot_pool_chunks()}} returns, or a
single unchunked run of the same shape.}
}
\value{
\code{invisible(TRUE)}. Called for the error it raises otherwise.
}
\description{
The runner that produces a bootstrap screen is a study file, not a templated
one, so the fields a report reads are a contract between two files that no
shared function enforces. This is that function.
}
\details{
It replaces the \code{contract} chunk that each bootstrap report carried, and it
differs from that chunk in the way that matters: \strong{it checks the shape of
every field it names, not merely that the name exists.} The chunk passed a
real bag happily while \code{requested} was a length-2 vector the report could not
render, and the resulting defect shipped in three releases.

Three shapes are checked, because three things can be true of a field:
\itemize{
\item \strong{Scalar} - \code{n_boot}, \code{seed}, \code{slentry}, \code{slstay}, \code{n_rows} and
\code{elapsed_mins} are each one value. A length-2 \code{n_boot} means the pool was
written per phase into a field that is not per phase, and every count
downstream is then ambiguous.
\item \strong{Per phase} - \code{requested} and \code{usable} may be a vector, and on a
multiphase screen they are: the candidate pool is \emph{offered} to each phase,
so \code{c(early = 230, late = 230)} is one pool seen twice. They must be
numeric and carry at least one value. \code{integer(0)} satisfies "is present"
and then contributes no element rather than one, which collapses any table
built from it.
\item \strong{Any shape} - \code{base_params} is read whole, and \code{boot} must carry
\code{replicates}, \code{summary}, \code{n_success} and \code{n_failed}. \code{boot} being present
says nothing about what is inside it. \code{manifest} is the one exception:
it must be a \strong{list}, because it is indexed by name, and
\code{manifest[["sha256"]]} on a named atomic vector lacking that name is not
\code{NULL} but an error -- \verb{subscript out of bounds}, naming neither the
field nor the file.
}

Every failure is reported at once. An author fixing a runner wants the whole
list; one at a time turns a single fix into five renders.

\code{dropped}, \code{free_sd}, \code{n_chunks}, \code{seeds} and the engine provenance fields
are \strong{not} required. A single unchunked run legitimately carries none of
them, and \code{\link[=boot_provenance]{boot_provenance()}}, \code{\link[=boot_health]{boot_health()}} and \code{\link[=boot_dropped]{boot_dropped()}} each say
what they do in their absence.

\code{free_sd} is the one of those with a shape to it: absent is fine, present
means a single value. It is the standard deviation of the \emph{first} free base
parameter, so one number is all it can be. A per-phase value validated
cleanly and then met \code{\link[=boot_health]{boot_health()}}'s scalar \code{if}, which R makes an error
naming neither the field nor the function.
}
\examples{
bag <- list(
  n_boot = 4L, seed = 101, slentry = 0.07, slstay = 0.05,
  base_params = "base", requested = 4L, usable = 3L,
  n_rows = 500L, elapsed_mins = 120, manifest = list(sha256 = "abc123"),
  boot = list(
    replicates = data.frame(replicate = c(1L, 2L),
                            parameter = c("early.age", "early.age"),
                            estimate = c(0.5, 0.6)),
    summary = data.frame(parameter = "early.age", n = 2L, pct = 50),
    n_success = 4L, n_failed = 0L))
boot_validate(bag)

# A per-phase `requested` is valid, not a defect: the pool is offered to
# each phase, so this is one pool seen twice.
bag$requested <- c(early = 230L, late = 230L)
boot_validate(bag)

# A per-phase `n_boot` is a defect, and this is the shape that shipped.
bag$n_boot <- c(500L, 500L)
try(boot_validate(bag))
}
\seealso{
\code{\link[=boot_provenance]{boot_provenance()}}, \code{\link[=boot_frequencies]{boot_frequencies()}}, \code{\link[=boot_concepts]{boot_concepts()}} and
\code{\link[=boot_health]{boot_health()}}, each of which calls this first; \code{\link[=boot_shortfall]{boot_shortfall()}} for
the different question of whether the pool is the run that was launched.
}
